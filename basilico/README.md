# ⚠️ Dossier généré automatiquement — ne pas modifier à la main

Ce dossier est l'export statique du site Next.js "Basilico", généré depuis
`../basilico-app/`. C'est ce que GitHub Pages sert en ligne.

Pour modifier le site : édite le code dans `../basilico-app/`, puis relance
`npm run build` là-bas et recopie le contenu de `out/` ici (voir le README
de `basilico-app/` pour les commandes exactes).

Voir en ligne : `https://mouradoulf-cmd.github.io/Mourad-site/basilico/`
