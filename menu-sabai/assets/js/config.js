// Menu Sabai — Supabase connection settings.
// Fill these in from Supabase → Project Settings → API.
// SUPABASE_ANON_KEY is safe to expose in client-side code (it only grants what the
// Row Level Security policies in supabase/schema.sql allow). Never put the
// service_role secret key in this file or anywhere in the browser.

const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
